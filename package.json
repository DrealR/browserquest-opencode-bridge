{
  "name": "browserquest-opencode-bridge",
  "version": "0.1.0",
  "private": false,
  "description": "Bridge to play BrowserQuest via AI CLIs like OpenCode. Provides an adapter API, MUD-style CLI client, and bot helpers.",
  "main": "src/server/index.js",
  "type": "module",
  "scripts": {
    "start:adapter": "node src/server/adapter.js",
    "start:cli": "node src/cli/bq-cli.js",
    "lint": "echo 'no lint configured'",
    "test": "node --test || echo 'no tests yet'"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "ws": "^8.18.0",
    "yargs": "^17.7.2"
  }
}